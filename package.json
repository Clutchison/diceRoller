{
  "name": "diceRoller",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx src/cli.ts -r",
    "build": "tsup src/cli.ts --format esm --target node20 --sourcemap --clean --dts --out-dir dist --splitting false --minify",
    "test": "vitest run",
    "prepare": "pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bin": {
    "diceRoller": "./dist/cli.mjs"
  },
  "dependencies": {
    "chalk": "^5.6.0",
    "commander": "^14.0.0",
    "random": "^5.4.1"
  },
  "devDependencies": {
    "@types/chai": "^5.2.2",
    "@types/node": "^24.3.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
